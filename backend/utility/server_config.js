// modulo che racchiude le credenziali di connessione al db per avere un codice più pulito e meno incasinato
let config = {
  db_address : "localhost",
  db_user : "server",
  db_password : "Server11",
  db_name : "IdsDB"
};
module.exports = config;
